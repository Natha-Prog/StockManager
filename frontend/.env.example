# Optionnel en dev : le proxy Vite redirige /api vers le backend
# VITE_API_URL=http://localhost:5000/api

# Production (Netlify) — URL de l'API Render :
# VITE_API_URL=https://stockmanager-backend.onrender.com/api
