# Optionnel en dev : le proxy Vite redirige /api vers le backend
# VITE_API_URL=http://localhost:5000/api

# Production (Vercel) — URL de l'API Render :
# VITE_API_URL=https://stockmanager-3.onrender.com/api
